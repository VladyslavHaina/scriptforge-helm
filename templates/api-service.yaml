apiVersion: v1
kind: Service
metadata:
  name: {{ include "scriptforge.fullname" . }}-api
spec:
  selector:
    app: {{ include "scriptforge.name" . }}
    component: api
  ports:
    - port: 80
      targetPort: 8080
